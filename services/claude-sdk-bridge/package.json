{
  "name": "claude-sdk-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "description": "Pieqi → Claude Agent SDK 常驻桥：HTTP/SSE 多轮会话 + 流式增量 + 审批挂起/释放 + resume（方案 multi-agent.md §5）",
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/bridge_self_test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.237"
  }
}
